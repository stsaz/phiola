# phiola global configuration

# Don't prevent the system from hibernating while playing or recording
AllowHibernate

# Code page for non-Unicode text
Codepage win1251

# Enable the deprecated ALAC decoder.
# It is disabled by default because the upstream library is unmaintained and has
# unpatched memory-safety vulnerabilities (see https://github.com/macosforge/alac).
# DeprecatedMods
